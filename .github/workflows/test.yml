name: Test package

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
  workflow_call:

env:
  PYTEST_ADDOPTS: "--color=yes"

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Setup Pixi
      uses: prefix-dev/setup-pixi@v0.8.8
    # Windows unit test is run separately because the full test suite takes ~1.5 hours compared to ~3mins on mac and Ubuntu.
    - name: windows unit test
      run: pixi run pytest -k 'test_Hc_Mr_BHmax_from_Ms_A_K_single_input[Ku0-A0-Ms0]'
      if: matrix.os == 'windows-latest'
    - name: unit tests
      run: pixi run test-unittest
      if: matrix.os != 'windows-latest'
    - name: doc tests
      run: pixi run test-docstrings
      if: matrix.os != 'windows-latest'
    - name: Test examples
      run: pixi run test-notebooks
      if: matrix.os != 'windows-latest'
    - name: Keep pixi.lock artifact for debugging
      uses: actions/upload-artifact@v4
      with:
        path: pixi.lock
      if: matrix.os == 'ubuntu-latest'

  build:
    needs: test
    name: Build sdist and wheel
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v5
      with:
        python-version: '3.13'
    - name: Install pypa/build
      run: python -m pip install --user build
    - name: Build wheel and source tarball
      run: python -m build
    - name: Store the distribution packages
      uses: actions/upload-artifact@v4
      with:
        name: dist
        path: dist/
