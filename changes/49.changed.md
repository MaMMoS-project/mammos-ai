Impose the beyond Stoner-Wohlfarth fixed-angle subpackage to use the entity `MagnetocrystallineAnisotropyConstantK1` and the letter `K1` instead of `Ku` and `K` as much as possible. Entities `UniaxialAnisotropyConstant` (`Ku`) are allowed but converted to `K1` internally.
